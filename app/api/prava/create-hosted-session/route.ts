import { createPravaSandboxSession, PravaApiError, PravaValidationError, type PravaCheckoutRequest } from "@/services/prava";
export const runtime = "nodejs";
export async function POST(request: Request) { try { const input = await request.json() as Partial<PravaCheckoutRequest>; console.info("[TrustLane Prava hosted validation]", { stage: "request parsed", message: "Hosted checkout request parsed." }); if (!input.merchant || !input.product || !input.amount || !input.currency || !input.decisionLedgerId) return Response.json({ error: "Incomplete checkout details.", code: "INVALID_REQUEST", status: 400 }, { status: 400 }); const session = await createPravaSandboxSession(input as PravaCheckoutRequest, "full_checkout"); return Response.json({ sessionId: session.session_id, orderId: session.order_id, iframeUrl: session.iframe_url, expiresAt: session.expires_at }); } catch (error) { if (error instanceof PravaValidationError) { console.info("[TrustLane Prava hosted validation]", { stage: "local validation failed", message: error.safeMessage }); return Response.json({ error: error.safeMessage, code: error.code, status: error.status }, { status: error.status }); } if (error instanceof PravaApiError) { console.error("[TrustLane Prava hosted session]", { status: error.status, requestId: error.requestId, code: error.code, message: error.sanitizedBody.message, body: error.sanitizedBody }); return Response.json({ error: "Unable to start hosted checkout.", code: error.code, status: error.status }, { status: 502 }); } if (process.env.NODE_ENV !== "production") console.error("[TrustLane Prava hosted session]", { stage: "unexpected failure", message: error instanceof Error ? error.message : "Unknown error" }); return Response.json({ error: "Unable to start hosted checkout.", code: "HOSTED_CHECKOUT_UNAVAILABLE", status: 503 }, { status: 503 }); } }
