import { createPravaSandboxSession, type PravaCheckoutRequest } from "@/services/prava";
export const runtime = "nodejs";
export async function POST(request: Request) { try { const input = await request.json() as Partial<PravaCheckoutRequest>; if (!input.merchant || !input.merchantUrl || !input.product || !input.amount || !input.currency || !input.decisionLedgerId) return Response.json({ error: "Incomplete checkout details." }, { status: 400 }); const session = await createPravaSandboxSession(input as PravaCheckoutRequest, "full_checkout"); return Response.json({ sessionId: session.session_id, orderId: session.order_id, iframeUrl: session.iframe_url, expiresAt: session.expires_at }); } catch (error) { if (process.env.NODE_ENV !== "production") console.error("[TrustLane Prava hosted session]", error); return Response.json({ error: process.env.NODE_ENV === "production" ? "Unable to start secure checkout." : error instanceof Error ? error.message : "Unable to start secure checkout." }, { status: 500 }); } }
