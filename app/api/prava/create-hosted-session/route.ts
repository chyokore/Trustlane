import { createPravaSandboxSession, PravaApiError, type PravaCheckoutRequest } from "@/services/prava";
export const runtime = "nodejs";
export async function POST(request: Request) { try { const input = await request.json() as Partial<PravaCheckoutRequest>; if (!input.merchant || !input.merchantUrl || !input.product || !input.amount || !input.currency || !input.decisionLedgerId) return Response.json({ error: "Unable to start hosted checkout.", code: "invalid_request", status: 400 }, { status: 400 }); const session = await createPravaSandboxSession(input as PravaCheckoutRequest, "full_checkout"); return Response.json({ sessionId: session.session_id, orderId: session.order_id, iframeUrl: session.iframe_url, expiresAt: session.expires_at }); } catch (error) { if (error instanceof PravaApiError) { console.error("[TrustLane Prava hosted session]", { status: error.status, requestId: error.requestId, code: error.code, message: error.sanitizedBody.message, body: error.sanitizedBody }); return Response.json({ error: "Unable to start hosted checkout.", code: error.code, status: error.status }, { status: 502 }); } const code = error instanceof Error && error.message.includes("merchant URL") ? "invalid_merchant_url" : "hosted_checkout_unavailable"; if (process.env.NODE_ENV !== "production") console.error("[TrustLane Prava hosted session]", { code, message: error instanceof Error ? error.message : "Unknown error" }); return Response.json({ error: "Unable to start hosted checkout.", code, status: 500 }, { status: 500 }); } }
