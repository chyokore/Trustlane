import { FileCheck2, Landmark } from "lucide-react";
import type { VerifiedMerchantContext } from "@/services/senso";

export function MerchantPassport({ context }: { context?: VerifiedMerchantContext }) {
  const available = context?.verificationStatus === "verified";
  return <section aria-label="Merchant Passport" className="rounded-xl border border-border bg-background/35 p-4"><div className="flex items-center gap-2"><Landmark className="size-4 text-primary" /><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-primary">Merchant Passport</p><p className="text-sm font-medium">{context?.merchant ?? "Selected merchant"}</p></div></div><div className="mt-3 grid gap-2 text-xs sm:grid-cols-2"><p className="rounded-lg bg-muted/35 p-2"><span className="block text-muted-foreground">Verification status</span><span className="font-medium">{available ? "Verified context returned" : "Unavailable"}</span></p><p className="rounded-lg bg-muted/35 p-2"><span className="block text-muted-foreground">Evidence sources</span><span className="font-medium">{context?.citations.length ?? 0} available</span></p><p className="rounded-lg bg-muted/35 p-2"><span className="block text-muted-foreground">Policy verification</span><span className="font-medium">{available ? "See grounded context" : "Unavailable"}</span></p><p className="rounded-lg bg-muted/35 p-2"><span className="block text-muted-foreground">Verification timestamp</span><span className="font-medium">{context?.verifiedAt ?? "Not supplied"}</span></p></div>{available && <p className="mt-3 flex items-start gap-2 text-xs leading-5 text-muted-foreground"><FileCheck2 className="mt-0.5 size-3 shrink-0 text-primary" />{context?.groundedAnswer}</p>}</section>;
}
