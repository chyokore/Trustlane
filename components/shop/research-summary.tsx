import { Clock3, Landmark, SearchCheck, Wallet } from "lucide-react";
import { VerifiedSensoContext } from "@/components/senso/verified-context";
import { MerchantPassport } from "@/components/senso/merchant-passport";
import type { VerifiedMerchantContext } from "@/services/senso";
const summary = [["Products compared", "18", SearchCheck], ["Verified merchants", "14", Landmark], ["Rejected merchants", "4", Landmark], ["Estimated savings", "$141", Wallet], ["Time researching", "4m 18s", Clock3]];
export function ResearchSummary({ context, loading }: { context?: VerifiedMerchantContext; loading?: boolean }) { return <aside className="rounded-2xl border border-border bg-card/60 p-5 xl:sticky xl:top-24"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-primary">Research Summary</p><h2 className="mt-1 font-semibold">Shopping with receipts</h2><div className="mt-5 space-y-3">{summary.map(([label, value, Icon]) => { const SummaryIcon = Icon as typeof Clock3; return <div className="flex items-center gap-3 rounded-xl bg-muted/35 p-3" key={label as string}><SummaryIcon className="size-4 text-primary" /><div className="min-w-0 flex-1"><p className="text-xs text-muted-foreground">{label as string}</p><p className="mt-0.5 font-semibold">{value as string}</p></div></div>; })}</div><div className="mt-5 rounded-xl border border-primary/20 bg-primary/5 p-3"><p className="text-sm font-medium">Verification is still live</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Prices, stock, and merchant signals are continuously checked before approval.</p></div><div className="mt-4"><VerifiedSensoContext context={context} loading={loading} /></div><div className="mt-4"><MerchantPassport context={context} /></div></aside>; }
