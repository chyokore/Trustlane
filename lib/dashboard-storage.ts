import type { CheckoutAttempt, LatestResearch, LocalPreferences, SavedProduct } from "@/types/dashboard-state";
const keys = { research: "trustlane.latest-research", orders: "trustlane.checkout-attempts", saved: "trustlane.saved-products", preferences: "trustlane.preferences" } as const;
const defaults: LocalPreferences = { language: "English", currency: "USD", notifications: true, approvalRequired: true, reducedMotion: false };
function read<T>(key: string, fallback: T): T { if (typeof window === "undefined") return fallback; try { const value = window.localStorage.getItem(key); return value ? JSON.parse(value) as T : fallback; } catch { return fallback; } }
function write<T>(key: string, value: T) { if (typeof window !== "undefined") window.localStorage.setItem(key, JSON.stringify(value)); }
export const dashboardStorage = { getResearch: () => read<LatestResearch | undefined>(keys.research, undefined), setResearch: (value: LatestResearch) => write(keys.research, value), getOrders: () => read<CheckoutAttempt[]>(keys.orders, []), setOrders: (value: CheckoutAttempt[]) => write(keys.orders, value), getSaved: () => read<SavedProduct[]>(keys.saved, []), setSaved: (value: SavedProduct[]) => write(keys.saved, value), getPreferences: () => read<LocalPreferences>(keys.preferences, defaults), setPreferences: (value: LocalPreferences) => write(keys.preferences, value) };
